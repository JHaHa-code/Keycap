import { Analytics } from '@vercel/analytics/next'
import type { Metadata, Viewport } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'

const geistSans = Geist({ subsets: ['latin'], variable: '--font-geist-sans' })
const geistMono = Geist_Mono({ subsets: ['latin'], variable: '--font-geist-mono' })

export const metadata: Metadata = {
  title: 'KeyClick Studio — Mechanical Keyboard Simulator',
  description:
    'A premium mechanical keyboard simulator in your browser. Feel Cherry MX, Gateron, Topre, Holy Panda and more with realistic sound, haptics and buttery-smooth key animations.',
  generator: 'v0.app',
  keywords: ['mechanical keyboard', 'switch tester', 'ASMR', 'Cherry MX', 'Topre', 'keyboard simulator', 'keycap'],
  openGraph: {
    title: 'KeyClick Studio — Mechanical Keyboard Simulator',
    description: 'Feel a real mechanical keyboard in your browser. Realistic switch sounds, haptics and 60fps animations.',
    type: 'website',
  },
}

export const viewport: Viewport = {
  colorScheme: 'light dark',
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: '#ffffff' },
    { media: '(prefers-color-scheme: dark)', color: '#0a0a0a' },
  ],
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning className={`${geistSans.variable} ${geistMono.variable} bg-background`}>
      <body className="antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
